{
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.2",
    "codacy-coverage": "^3.0.0",
    "crypto": "^1.0.1",
    "ejs": "^2.5.9",
    "express": "^4.16.3",
    "express-fileupload": "^1.0.0",
    "fs": "0.0.2",
    "istanbul": "^0.4.5",
    "jasmine-node": "^1.14.5",
    "request": "^2.85.0",
    "sitemap": "^2.1.0"
  },
  "scripts": {
    "test": "jasmine-node spec",
    "test-with-coverage": "istanbul cover jasmine-node --captureExceptions spec/ --include-all-sources --root ./src && cat ./coverage/lcov.info | codacy-coverage && rm -rf ./coverage"
  }
}
